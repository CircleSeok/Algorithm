function solution(numbers) {
    let answer = numbers.reduce((x, y)=> (x, y*2), []);
    return answer
}

function solution(numbers) {
    let arr = numbers.map(number => number *2)
    return arr



}